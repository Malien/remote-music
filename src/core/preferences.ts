let fs = require('fs');

export class Preferences {
    server, 
}

export class PreferenceLoader {
    
    readPref : function (path: String) -> Preferences {
        try {
            
        } catch (error) {
            
        }
    }
}